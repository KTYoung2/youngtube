mixin video(video)
    div 
        h4
            a(href="/videos/" + video.id)=video.title
        p=video.description
        ul
            each hashtag in video.hashtags 
                li=hashtag
        span #{video.owner.name} 님 
        br
        span 조회수 #{video.meta.views} 회
        br
        span=video.createdAt 
        hr